# 帕金森运动量化评估

该项目描述门诊评估方案、双腕采集试次、用药相位和派生指标。原始试次与每次算法运行的关系需要保持稳定，以便医生比较不同访视。

`src/contracts.ts` 包含方案、试次和指标运行的领域结构。`fixtures/motor-assessments.json` 提供两次脱敏访视，覆盖设备不同步、试次中断和补做。

运行环境是 Node.js 22，类型系统使用 TypeScript；`npm test` 用于检查契约一致性。
